-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.app_settings (
  key text NOT NULL,
  value jsonb NOT NULL,
  CONSTRAINT app_settings_pkey PRIMARY KEY (key)
);
CREATE TABLE public.contracts (
  id text NOT NULL,
  year_month text NOT NULL,
  source_file text NOT NULL,
  key_box_number text,
  status_date date,
  reins_change_date date,
  reins_changed boolean DEFAULT false,
  reins_expire_date date,
  reins_registered boolean DEFAULT false,
  cancel_reason text,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  updated_by text,
  notes text,
  media_source text,
  deal_status text,
  seller_name text,
  seller_address text,
  seller_contact text,
  mediation_expire_date date,
  mediation_start_date date,
  staff_id text,
  property_address text,
  property_type text,
  current_price integer,
  occupancy_status text,
  application_date date,
  contract_type text,
  key_location text,
  price_history jsonb,
  change_history jsonb,
  deal_info jsonb,
  purchase_info jsonb,
  CONSTRAINT contracts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  category text NOT NULL,
  year integer NOT NULL,
  case_number text NOT NULL,
  status text,
  staff_id text,
  inquiry_date date,
  inquiry_source text,
  contact_method text,
  property_type text,
  target_property text,
  assessment_address text,
  desired_property text,
  customer_name text,
  phone text,
  current_address text,
  email text,
  first_call text,
  call_status text,
  mail_status text,
  sms_status text,
  showing_status text,
  pre_assessment text,
  visit_status text,
  mediation_status text,
  contract_status text,
  expected_yield text,
  expected_rent text,
  self_funds text,
  desired_loan text,
  preferred_area text,
  memo text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  billing_exclusion text,
  exclusion_data jsonb DEFAULT '{}'::jsonb,
  postal_status text,
  CONSTRAINT customers_pkey PRIMARY KEY (id)
);
CREATE TABLE public.record_locks (
  id bigint NOT NULL DEFAULT nextval('record_locks_id_seq'::regclass),
  resource_type text NOT NULL,
  resource_id text NOT NULL,
  locked_by text NOT NULL,
  locked_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL,
  CONSTRAINT record_locks_pkey PRIMARY KEY (id)
);